from urllib.parse import urlparse


def transliterate(input_str):
    # make something with input_str
    # 1. Удалить пробелы слева, справа
    # 2. Заменить все пробелы внутри строки на _
    # 3. Убрать спецсивмолы, либо заменить их на URL валидные => urlparse
    # 4. Заменить русские буквы на английские
    return input_str
